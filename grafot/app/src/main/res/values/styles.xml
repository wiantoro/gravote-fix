<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColorPrimary">@color/biru</item>
        <item name="colorPrimary">@color/biruMuda</item>
        <item name="colorPrimaryDark">@color/biruTua</item>
        <item name="colorAccent">@color/biruMuda</item>
        <item name="colorControlHighlight">@color/indigo</item>
        <item name="colorButtonNormal">@color/putih</item>

    </style>

</resources>
